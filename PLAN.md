# CardMaster — Piano di sviluppo

App per conservare e mostrare i codici a barre delle **carte fedeltà**. Offline-first.

**Strategia di rilascio:**
- **v1 — core offline, con un solo tassello online opt-in.** Aprire, scansionare e condividere le carte gira interamente sul device: salvataggio locale, sblocco biometrico, condivisione tramite **QR code self-contained** (contiene tutti i dati della carta, letto da un altro device). Nessuna auth online, nessuna sincronizzazione. L'unica funzione di rete è il **backup su Google Drive** (`maui-backup-drive`): opt-in, disattivabile, isolato dietro interfacce — se disabilitato o senza rete l'app resta 100% funzionale offline. *(Decisione 25 lug 2026: il backup Drive, inizialmente rimandato a v2, rimpiazza `maui-backup-local` e torna in v1 — vedi `openspec/changes/maui-backup-drive`.)*
- **v2 — backend e sincronizzazione (rimandato).** Si aggiunge il backend .NET 10 per auth online, sincronizzazione multi-device e account applicativo. Progettiamo la v1 in modo che questa evoluzione sia possibile senza migrazioni dolorose.


---

## Vincoli architetturali (decisi — non rimettere in discussione)

Questi vincoli vanno nel file di contesto di progetto di OpenSpec; ogni change deve rispettarli.

### v1 (core offline + backup Drive opt-in)
- **Prodotto**: app Android. Il core (aprire una carta, scansionarne una nuova, condividerla) funziona sempre senza rete e senza account. L'unica funzione online è il **backup su Google Drive**, opt-in e disattivabile (vedi `maui-backup-drive`).
- **Client**: .NET MAUI (solo Android). SQLite locale **in chiaro** (`SQLitePCLRaw.bundle_e_sqlite3`). Scansione con **ML Kit** (`BarcodeScanning.Native.Maui`), rendering barcode con **ZXing.Net + SkiaSharp**. *(Decisione 24 lug 2026: rimossa la cifratura SQLCipher — il pacchetto `bundle_e_sqlcipher` è deprecato e la cifratura non è ritenuta essenziale per la v1. Eventuale reintroduzione: `SQLite3MC.PCLRaw.bundle`, mantenuto.)*
- **Autenticazione v1**: **nessuna gate di sblocco applicativa**. L'app si apre direttamente sulla lista carte (nessun account, nessun server, nessun prompt biometrico o PIN). La protezione "telefono in mano ad altri" è delegata al lockscreen di Android. *(Decisione del 24 lug 2026: rimossi biometria e PIN applicativi rispetto all'ipotesi iniziale — l'app deve mostrare subito le carte.)*
- **Modello dati**: le carte sono locali al device. `Id` carte generati dal **client** (GUID/ULID) — così restano validi quando in v2 arriverà la sync. Cancellazioni logiche con **tombstone** (mai DELETE fisico), già ora, per non complicare la futura sincronizzazione.
- **Condivisione v1**: una carta si condivide mostrando un **QR code self-contained** che incapsula tutti i dati necessari (emittente/nome, `barcode`, `barcodeFormat`, eventuale colore/logo id). L'altro device lo **scansiona** e crea una **copia indipendente** (nessun legame persistente — il barcode fedeltà è immutabile). Peer-to-peer, **nessun server**. Alla ricezione: controllo duplicati (stesso emittente + barcode) per proporre di saltare invece di duplicare. Payload versionato (`v` nello schema) per compatibilità futura.
- **Riconoscimento emittente**: catalogo emittenti **bundle locale statico** (seed nell'app) per nome/logo/formato atteso; nessuna sync del catalogo in v1.
- **Sicurezza v1**: DB SQLite locale **in chiaro** (nessuna cifratura at-rest in v1); la protezione è delegata al lockscreen di Android. Niente TLS/E2E perché non c'è rete in gioco.
- **Repository GitHub pubblico**: *(Decisione 25 lug 2026, change `maui-auto-update`)* il repository `ianselmi/cardmaster` è **pubblico** — necessario perché l'auto-update legge direttamente le API Releases di GitHub (repo privato le renderebbe inaccessibili senza un token, non incorporabile in sicurezza nell'APK; GitHub Pages come alternativa è a pagamento per repo privati). Verificato che repo e cronologia non contengono segreti prima del cambio di visibilità. **Da qui in avanti: prima di ogni commit/push, rivedere il diff per escludere segreti o dati sensibili** (chiavi, token, credenziali, percorsi/dati personali) — il repo è ora visibile a chiunque. Il keystore di firma e le credenziali CI restano *solo* come secret GitHub Actions, mai nel repo.

### v2 (backend — riferimento, per non chiudere porte ora)
- **Backend**: .NET 10, **Minimal API**, **CQRS a vertical slice** (una cartella per feature: comando + handler + validator). EF Core + SQLite, **WAL**, scritture **serializzate da una coda** (SQLite = un solo writer). Niente read model separato, event sourcing o MediatR. **Docker + docker compose**, reverse proxy per il TLS.
- **Autenticazione v2**: primo accesso online (credenziali o **Google** con Authorization Code + PKCE, nessun client secret nell'APK) → access token breve + refresh token lungo con rotazione in `SecureStorage`. La biometria resta solo sblocco locale. Refresh token scaduto → app usabile in sola lettura, si blocca solo la sync.
- **Sincronizzazione v2**: cursore = contatore monotono `Seq` assegnato dal server (`INTEGER PRIMARY KEY AUTOINCREMENT` su `ChangeLog` append-only). Timestamp client solo come tie-breaker per il **last-write-wins**, mai cursore. `OperationId` client-generato per l'idempotenza sui retry.
- **Condivisione v2**: oltre al QR, eventuale link con snapshot lato server (token 128 bit da `RandomNumberGenerator`, TTL breve, rate limit, Android App Links + `assetlinks.json`).

---

## Fase 0 — Setup OpenSpec

- [ ] Verificare Node.js ≥ 20.19.0
- [ ] `npm install -g @fission-ai/openspec@latest`
- [ ] `openspec init --tools claude` nella cartella di progetto
- [ ] `openspec --help` per verificare che i comandi slash corrispondano (OpenSpec si muove veloce)
- [ ] Popolare il **file di contesto di progetto** con i vincoli architetturali qui sopra (senza scrivere codice né creare change in questo passaggio)
- [ ] Verificare il risultato con `git diff` prima di procedere

**Ciclo per ogni change** (una per sessione, contesto pulito tra una e l'altra):
`/opsx:propose <nome>` → leggi e correggi la proposta → `openspec validate <nome>` → `/opsx:apply` → `openspec archive <nome>`.
Per le change dove il "come" non è ovvio (es. `maui-unlock`) partire da `/opsx:explore`.
⚠️ Non proporre tutte le change in una volta: ognuna va scritta conoscendo le precedenti già implementate.

---

## v1 — App offline + backup Drive opt-in

- [x] **`maui-shell`** — progetto MAUI Android, navigazione, DI, SQLite locale con SQLCipher e chiave in Keystore
- [~] **`maui-unlock`** — ~~biometria via `BiometricPrompt`, fallback PIN, gestione invalidazione della chiave al cambio impronte~~ **ANNULLATA** (24 lug 2026): decisa nessuna gate di sblocco: l'app apre subito le carte. La chiave del DB resta nel Keystore senza binding all'autenticazione utente (come già in `maui-shell`).
- [x] **`issuer-seed`** — catalogo emittenti come seed statico bundle nell'app (nome, logo, colore, formato barcode atteso); nessuna sync
- [x] **`maui-scan-card`** — scansione ML Kit, formati EAN-13/EAN-8/UPC-A/UPC-E/Code128/Code39/ITF/Codabar/QR/PDF417, inserimento manuale, emittente opzionale dal seed, avviso duplicati, salvataggio locale (Id client-generato, tombstone)
- [x] **`maui-card-grid`** — lista carte come griglia di riquadri (2 colonne, quadrettoni con angoli arrotondati), colore di sfondo generato in modo deterministico per carta
- [x] **`maui-show-card`** — rendering del barcode (ZXing.Net + SkiaSharp), luminosità al massimo e blocco spegnimento schermo, codice in chiaro come fallback, avviso filtro luce blu best-effort
- [x] **`maui-card-search`** — ricerca tra le carte (per nome/emittente) e barra aggiuntiva con le **ultime 3 carte usate** (richiede tracciare un timestamp "ultimo utilizzo" all'apertura della carta)
- [x] **`maui-edit-card`** — modifica dei dati di una carta esistente (nome, emittente, colore; eventualmente barcode/formato), con salvataggio via repository (aggiorna `UpdatedAt`)
- [x] **`maui-restyle`** — restyle grafico complessivo: nuovo **logo** (app icon + splash) e **palette colori** (tema/accent, colori dei tile), coerenza di tipografia e spaziature sulle pagine esistenti
- [x] **`maui-share-qr`** — genera un QR code self-contained con i dati della carta (payload versionato); import scansionando il QR di un altro device; controllo duplicati alla ricezione
- [x] **`maui-settings`** — sezione **Impostazioni**: pagina raggiungibile dalla lista carte (icona ruota dentata in toolbar), store delle preferenze (MAUI `Preferences`), info app (versione), e **preferenza tema** Sistema/Chiaro/Scuro persistita e applicata all'avvio. Predisposta come host della futura opzione di backup (implementata da `maui-backup-drive`)
- [x] **`maui-backup-drive`** — backup/ripristino del database su **Google Drive** come **unica funzione di rete della v1**, opt-in e disattivabile: Google Sign-In (OAuth Authorization Code + PKCE, nessun client secret nell'APK), snapshot (`VACUUM INTO`) caricato come blob db3 opaco nella cartella `appdata`, ritenzione ultimi 3, ripristino con guardia di versione schema e snapshot di sicurezza, schedulazione (Mai/A ogni apertura/Giornaliero/Settimanale), notifica di avanzamento stile WhatsApp. *(Decisione 25 lug 2026: rimpiazza l'idea `maui-backup-local` — file esportabile — e viene anticipato da v2 a v1, vedi `openspec/changes/maui-backup-drive`. OAuth client Android creato su Google Cloud Console e login verificato in produzione il 25 lug 2026 — vedi `docs/google-drive-backup.md`, incluso il passaggio "Enable custom URI scheme" necessario oltre a package name/SHA-1.)*
- [x] **`ci-build-apk`** — pipeline di build (GitHub Actions) che compila l'app MAUI Android, **firma** l'APK con keystore (secret CI), e pubblica l'artifact/APK come GitHub Release; versionamento automatico (`ApplicationVersion`/`ApplicationDisplayVersion`). *(Prerequisito utente: creare keystore + secret — vedi `docs/ci-release.md`.)*
  - [x] **`ci-release-app-version`** — il titolo della prerelease `latest` su `main` mostra il versionName dell'app (`ApplicationDisplayVersion`) invece dell'etichetta fissa "Ultima build (main)", così release e versione installata coincidono.
  - [x] **`ci-release-version-tags`** — la versione dell'app diventa il **numero di build incrementale** (`8`, `9`, `10`…); ogni build su `main` crea una Release versionata taggata col numero di build (storico con APK) oltre alla prerelease `latest` sull'ultima. Tag senza prefisso `v`; ramo `v*` invariato.
- [x] **`maui-auto-update`** — controllo aggiornamenti su richiesta dalle Impostazioni, interrogando direttamente la **Release GitHub con tag `latest`** (API pubbliche, repo pubblico — vedi decisione sopra); se diversa dalla versione installata, scarica l'APK con avanzamento (notifica + foreground service), **verifica il checksum** quando disponibile, e lancia l'installazione via package installer intent (`REQUEST_INSTALL_PACKAGES` + `FileProvider`). Funzione online opzionale: non tocca il core offline. Verificato end-to-end su emulatore il 25 lug 2026.
- [x] **`maui-update-notify`** — segnale in-app della presenza di un aggiornamento, fuori dalla pagina Impostazioni (badge sull'icona Impostazioni + banner dismissibile in lista carte): opzione opt-in "Avvisami di nuove versioni" (default disattivata) che abilita un controllo automatico limitato a 1 volta/24h e legato al solo foreground dell'app (mai in background/silenzioso); segnale silenziabile per versione, ricompare solo per una versione remota successiva. Riusa integralmente il client di controllo di `maui-auto-update`, nessuna nuova infrastruttura. Verificato end-to-end su emulatore il 25 lug 2026.
- [x] **`maui-settings-backup-status`** — il pulsante "Backup su Google Drive" nella pagina Impostazioni segnala se il backup è attivo (sottotitolo di stato + colore diverso del pulsante), senza dover entrare nella sezione dedicata. Riusa `ISettingsStore.BackupEnabled` già esistente, nessuna modifica al comportamento di `cloud-backup`. `dotnet build` verificato; stato di default verificato su emulatore, stato "attivo" non verificabile end-to-end sull'emulatore (nessun account Google per completare l'OAuth).
- [x] **`maui-scan-from-image`** — terzo percorso di acquisizione a fianco di camera e inserimento manuale: si sceglie un'immagine già sul device (screenshot, foto della tessera, QR di condivisione ricevuto in chat) e ML Kit ne estrae il barcode, sboccando nella stessa schermata di conferma. Riusa il motore e il catalogo formati della scansione live e il codec di condivisione (`ICardShareCodec`), quindi anche un QR CardMaster in un'immagine viene importato come snapshot completo; nessuna nuova dipendenza, nessuna rete. Selettore via `FilePicker` (SAF) proprio per non introdurre permessi di storage: manifest unito verificato, `READ_EXTERNAL_STORAGE`/`READ_MEDIA_IMAGES` assenti. `dotnet build` verificato (0 errori); **verifica funzionale su emulatore non eseguita** (interrotta su richiesta) — da fare al primo giro sull'app.
- [x] **`maui-card-list-compact-header`** — l'area sopra la griglia (ricerca, conteggio, chip label, recenti) era cresciuta per accumulo fino a occupare il **38%** dell'altezza utile (51% col banner). Compattata: il conteggio perde la riga dedicata e compare **solo sotto filtro**, condividendo la riga dei chip; via la didascalia "Usate di recente" (la barra resta invariata); altezze di chip e recenti dalla scala di spaziatura condivisa. La testata passa da righe di `Grid` a `VerticalStackLayout`, che dispone solo i figli visibili: risolve alla radice il residuo di `RowSpacing` lasciato dalle righe nascoste, difetto già notato in `maui-card-color-labels`. Risultato misurato su emulatore: **18%** senza label, **23%** con label, **28%** col banner — contro un obiettivo di ≤33%. `dotnet build` 0 errori; verificato il 27 lug 2026.
- [x] **`maui-update-system-notification`** — la disponibilità di una nuova versione arriva anche come **notifica di sistema Android**, non solo come banner/badge in-app: canale esistente `cardmaster_update` con id dedicato (così non si sovrappone a quella di download), importanza Low, `ContentIntent` che apre il flusso di aggiornamento. Il controllo automatico diventa anche **periodico ad app chiusa** (WorkManager **orario** con vincolo di rete, stesso modello del backup Drive; l'intervallo minimo tra due controlli scende da 24 ore a 1 ora per restare allineato al periodo — altrimenti il worker girerebbe a vuoto): *(Decisione 27 lug 2026: **ribalta** il requisito di `app-update-notify` che vietava i controlli fuori dal foreground — senza background la notifica arriverebbe solo ad app aperta, cioè quando non serve.)* Nessuna nuova opzione: governa tutto lo switch esistente "Avvisami di nuove versioni", che ora richiede anche il permesso notifiche all'attivazione (prima era chiesto solo abilitando il backup Drive). Notifica ripetuta a ogni controllo finché l'aggiornamento è pendente, salvo silenziamento della versione. Disattivando l'opzione: work annullato e notifica rimossa. `dotnet build` 0 errori; verificato su emulatore il 27 lug 2026.
- [x] **`maui-update-stale-signal`** — *bugfix*: dopo aver installato un aggiornamento l'app continuava a segnalare come disponibile **quella stessa versione** (banner in lista, badge Impostazioni, riga "Ultimo controllo"). Causa: il segnale era derivato da "esiste un esito di controllo memorizzato" invece che da "la versione rilevata è diversa da quella installata", e `LastUpdateCheckAvailableVersion` è una preferenza persistita che sopravvive all'aggiornamento; l'unico punto che l'azzerava era un controllo di rete "sei aggiornato", che con l'opzione opt-in disattivata (default) non parte mai. Correzione: `IUpdateService.ReconcileInstalledVersion()`, **locale e senza rete**, invocata in `OnStart`/`OnResume` prima del controllo opt-in, più un punto di verità unico (`AvailableUpdateVersion`) già filtrato rispetto alla versione installata. `LastUpdateCheckUtc` resta intatto (un controllo è avvenuto davvero). Sana da sé i device già in stato sporco. `dotnet build` 0 errori; **bug riprodotto e correzione verificata su emulatore** il 27 lug 2026, incluso il caso offline con opzione disattivata.
- [x] **`maui-card-color-labels`** — nella modifica (e nella creazione) di una carta si sceglie il **colore del riquadro** da una palette curata, con l'opzione "Automatico" che ripristina il colore derivato dal nome, e si assegnano **label** libere (create al volo, con suggerimenti dalle label già in uso, dedup case/accent-insensitive, max 8 per carta). Nella lista compare una riga di **chip** per filtrare per label a selezione multipla in **OR**, combinata in AND con la ricerca testuale. Colore utente in un campo `TileColor` **distinto** dal `Color` di brand ereditato dall'emittente, così le carte esistenti non cambiano aspetto; label serializzate su una colonna della carta (niente tabelle molti-a-molti, che sarebbero il caso peggiore per il last-write-wins per riga previsto in v2). Schema DB a **v2** (colonne aggiunte da `CreateTableAsync`, l'incremento serve alla guardia del backup Drive). Nessuna label nel payload QR di condivisione. `dotnet build` 0 errori; verificato su emulatore end-to-end il 27 lug 2026, **incluso l'aggiornamento sopra la versione precedente** (carte esistenti con colori identici e nessun chip).
- [x] **`maui-card-list-add-fab`** — l'aggiunta carta lascia la voce testuale "Aggiungi" in alto a destra (angolo scomodo col pollice, accanto a Impostazioni) e diventa un **bottone tondo con il `+` in basso al centro**, 56 dp come il FAB Android standard. Sovrapposto alla griglia come secondo figlio della *stessa* cella di `Grid`, non in una riga propria: non sottrae altezza alle carte — quella appena recuperata da `maui-card-list-compact-header` — e resta fermo mentre la lista scorre. Lo spazio perché l'ultima riga non finisca sotto al bottone è un `CollectionView.Footer` trasparente e non un `Padding`: `ItemsView` non espone `Padding` (errore `MAUIX2002`) e un `Margin` ridurrebbe il viewport invece di riservare spazio scrollabile. `Button` con `CornerRadius` metà del lato invece di `Border`+`TapGestureRecognizer`, per non perdere il feedback al tocco dello stile implicito. `dotnet build` 0 errori; verificato su emulatore il 28 lug 2026 (cerchio col `+` centrato, tocco che apre la scansione, ultima riga non coperta, presente a lista vuota e a filtro senza risultati, tema chiaro e scuro).
- [x] **`maui-show-card-labels`** — le label di una carta si vedevano solo dove si cambiano (la schermata di modifica): per sapere com'era taggata una carta bisognava aprirla in scrittura e tornare indietro. Ora compaiono anche nel **dettaglio**, in sola lettura, come chip sotto l'area barcode e sopra i pulsanti — sotto e non sopra, così una carta con molte label non spinge in basso il barcode, che è il motivo per cui quella pagina esiste. Sezione nascosta per intero se la carta non ha label (figlio invisibile in `VerticalStackLayout`: niente spazio, niente spaziatura). Nessuna query in più: le label arrivano con la carta già caricata, e il refresh dopo una modifica è già garantito dal `ReloadAsync()` in `OnAppearing`. Chip replicati e non estratti in un controllo condiviso: rispetto all'editor cambiano rimozione, gesture e ViewModel. `dotnet build` 0 errori; verificato su emulatore il 28 lug 2026 (7 label che vanno a capo, carta senza label, tocco che non modifica nulla, aggiornamento dopo la modifica, tema chiaro e scuro).
- [x] **`maui-show-card-name-badge`** — il nome della carta compariva **solo nella barra del titolo**, che il sistema tronca sui nomi lunghi e che alla cassa — telefono girato verso il cassiere — è la parte di schermo che si guarda meno: il riquadro bianco che si porge non diceva a quale carta appartenesse. Ora il nome sta **dentro quel riquadro**, come primo figlio sopra il barcode, su una **fascia a tutta larghezza** col colore identitario della carta (`CardTilePalette.ForCard`, lo stesso della griglia: scelto dall'utente o derivato dal nome) e testo bianco in grassetto. Il colore arriva da un `TileColor` proiettato dal `ShowCardViewModel`, non da un converter sull'entità `Card`: stesso stile degli altri campi già proiettati, e la regola "colore utente altrimenti derivato" non viene duplicata. Fascia a tutta larghezza e non pillola aggrappata al testo, che su nomi lunghi crescerebbe comunque fino al bordo con esito incoerente tra carte. Il vincolo che conta resta intatto: area barcode e codice in chiaro **su fondo bianco anche in tema scuro**, colorata solo la riga del nome. `dotnet build` 0 errori; verificato su emulatore il 28 lug 2026 (colore derivato uguale al riquadro in griglia, colore scelto dall'utente, nome lungo su due righe con barcode non coperto mentre il titolo di sistema tronca, tema scuro, titolo invariato).
- [x] **`maui-backup-error-state`** — quando un backup su Drive falliva, o il refresh token veniva revocato, l'app non lo diceva: la pagina Backup restava identica a una perfettamente funzionante e l'unico segnale era un alert momentaneo col testo grezzo di Google (`Drive ha risposto 403: {json}`), mentre per i backup schedulati l'esito spariva in una notifica di sistema. Ora l'esito dell'**ultimo tentativo** (categoria d'errore + timestamp) è persistito nelle preferenze e mostrato come **banner permanente** in pagina, accanto alla data dell'ultimo backup **riuscito** — è la coppia delle due date a smontare l'illusione che i backup stiano funzionando. Cinque categorie (`BackupErrorKind`: rete, spazio Drive esaurito, credenziali, servizio, errore locale) classificate alla fonte nel client Drive — un 403 si distingue tra "spazio finito" e rate limit leggendo `error.errors[].reason`, non il solo status — e tradotte in messaggi con "cosa è successo + cosa fare"; il testo tecnico resta dettaglio interno. Per le credenziali non più valide, azione **"Riconnetti l'account Google"** = solo `SignInAsync` senza `SignOutAsync`, che sovrascrive il refresh token morto mantenendo abilitazione, frequenza e backup su Drive (passare da `DisableAsync`+`EnableAsync` azzererebbe tutto). Corretto per strada un difetto latente: `GetValidAccessTokenAsync` restituiva `null` sia per rete assente sia per token rifiutato e il client traduceva **entrambi** in "credenziali scadute" → falsi inviti a riconnettersi in assenza di campo; ora `AccessTokenResult`/`TokenFailure` separano i due casi. Il pulsante in Impostazioni distingue tre stati (non attivo / attivo / attivo ma non funzionante). `dotnet build` 0 errori; verificato su emulatore il 3 ago 2026: percorso d'errore reale (backup senza credenziali → messaggio comprensibile, nessun JSON), persistenza dell'esito dopo riavvio dell'app, banner di riconnessione e segnale rosso in Impostazioni nelle due varianti. **Non verificabili end-to-end sull'emulatore** (nessun account Google): classificazione live di `StorageFull`/`Service`/`Network` e riconnessione OAuth completa.
- [x] **`maui-restore-pre-backup-drive`** — prima di sostituire il database, il ripristino salvava la situazione corrente in uno **snapshot locale in cache** tenuto per un comando "Annulla ripristino": una seconda rete di sicurezza con regole tutte sue, invisibile (non compare nella lista dei backup), cancellabile da Android insieme alla cache e viva solo finché il processo dell'app resta in piedi. Ora la situazione corrente finisce su Drive come **backup ordinario** — stesso nome, stessa ritenzione ≤3, elencabile e ripristinabile come qualunque altro, anche a giorni di distanza o da un altro device — e spariscono snapshot locale, undo e rollback automatico. Se quel backup **non riesce** (rete, spazio, credenziali) il ripristino **non parte** e il database resta intatto (`RestoreOutcome.PreBackupFailed`, categoria d'errore in chiaro all'utente): un ripristino richiede comunque la rete, quindi bloccarlo non toglie nulla. Dettaglio non ovvio: il **download del backup scelto precede** il backup della situazione corrente, perché quest'ultimo consuma uno slot di ritenzione e la rotazione può eliminare proprio il file che si sta per ripristinare (caso di chi ripristina il più vecchio dei 3) — con la copia già in cache la sua sparizione da Drive è irrilevante. Prezzo accettato: se la sostituzione del file si interrompe a metà il recupero passa da automatico a manuale (ripristinare il backup appena creato), indicato esplicitamente nel messaggio d'errore. `dotnet build` 0 errori; **verifica su emulatore non eseguita** (il flusso di ripristino richiede un account Google collegato e backup reali su Drive) — da fare al primo giro sull'app.
- [x] **`claude-md-agent-context`** — `CLAUDE.md` alla radice come punto d'ingresso per agenti AI, con riferimenti a `PLAN.md`/`docs/`/`openspec/` invece di duplicarli.
- [x] **`readme-doc`** — `README.md` pubblico alla radice: presentazione del progetto, download dell'ultima build, build locale, rimando a questo piano.

### Scontrini e analisi spese (proposto 11 ago 2026)

Feature grossa — di fatto un'app dentro l'app — divisa in quattro change a **rischio decrescente**: ognuna è verificabile da sola e la successiva non parte se la precedente non ha retto su scontrini veri. Solo `receipt-capture` è proposta; le altre tre sono qui come roadmap e vanno scritte conoscendo le precedenti già implementate.

**Test automatici (11 ago 2026):** con questo dominio entra in repo il primo progetto di test, `tests/CardMaster.Tests` (`dotnet test`). Copre la logica **pura** — estrazione della testata e ricostruzione delle righe dalla geometria dell'OCR — che è dove vivono gli errori silenziosi: un totale preso dalla riga sbagliata o una data spostata di un giorno non fanno crashare niente. Tre dei difetti trovati durante `receipt-capture` sono stati presi da questi casi. I sorgenti sotto test sono **collegati**, non copiati (vedi `CLAUDE.md`).

**Vincolo trasversale, deciso 11 ago 2026:** nessuna chiave, token o credenziale nel codice sorgente o nel pacchetto dell'applicazione, né in chiaro né offuscata — il repository è pubblico e l'APK è scaricabile da chiunque, quindi un segreto incorporato è un segreto compromesso. Quando servirà una chiave per l'IA sarà **la chiave dell'utente**, incollata da lui in Impostazioni e conservata in `SecureStorage` (mai in `Preferences`, mai nel DB, quindi nemmeno nel backup Drive). *(Scartato il proxy verso un backend: il backend .NET 10 è tutto in v2 e non esiste — il backup Drive parla direttamente da app a Google.)*

- [x] **`receipt-capture`** — acquisizione di uno scontrino da **foto** o da **immagine** già sul device, riconoscimento del testo **on-device** (ML Kit Text Recognition, variante con modello incluso nell'APK: funziona al primo avvio senza rete e senza Play Services — **costo misurato +9,8 MB, da 48,9 a 58,7 MB**, che l'auto-update riscarica a ogni versione; compromesso accettato per non tradire l'offline-first), estrazione con regole di **esercente, P.IVA, data e totale**, schermata di conferma dove ogni campo è correggibile a mano, storico degli scontrini e spesa per negozio/mese. Conserva il **testo integrale** per poter ri-estrarre i dati a regole migliorate senza rifotografare nulla. Importi in **centesimi interi**, formattazione euro fissa e non dalla cultura del device. Sezione "Scontrini" come secondo `ShellContent` **dentro un `TabBar`** (senza `TabBar` Shell costruisce un menu a panino, non la barra in basso). Schema DB a **v3**. Le immagini stanno su filesystem e **non sono nel backup Drive** — limite dichiarato nella pagina Backup e reso requisito nella delta spec di `cloud-backup`. Nessuna rete, nessun segreto.
  **Tre difetti emersi solo dalla prova su OCR reale**, che il testo di prova scritto a mano non poteva mostrare: (1) ML Kit non restituisce righe ma **blocchi**, e su uno scontrino a colonne le descrizioni precedono tutti gli importi — `TOTALE COMPLESSIVO` e il suo `6,61` a quindici righe di distanza, totale non riconosciuto pur essendo leggibile: le righe si ricostruiscono dalla **geometria** (`ReceiptTextLayout`, per banda verticale), che era l'algoritmo previsto per `receipt-items` e serve già qui perché senza non è estraibile nemmeno la testata — ed è la ragione per cui `IReceiptOcr` espone le coordinate fin da subito; (2) l'OCR **spezza i gruppi di cifre** (`11/08/ 2026`, `-0, 50`), quindi le regex tollerano spazi attorno ai separatori; (3) **tutte le date indietro di un giorno** perché sqlite-net persiste `DateTimeOffset` come tick UTC e la mezzanotte locale `+02:00` tornava come le 22:00 del giorno prima — errore silenzioso che a fine mese avrebbe spostato lo scontrino nel mese sbagliato. Tutte in `docs/technical-notes.md`.
  `dotnet build` 0 errori. Verificato su emulatore l'11 ago 2026: **aggiornamento sopra la versione precedente** con una carta già presente (carta intatta, tabella creata, `user_version` a 3), **3 catene diverse** con esercente/P.IVA/data/totale corretti e totali del mese esatti, **installazione pulita in modalità aereo al primo utilizzo in assoluto** (il test che giustifica il modello incluso), manifest unito senza permessi di storage. **Non verificati**: percorso fotocamera (l'emulatore non inquadra uno scontrino vero), modifica/eliminazione di uno scontrino salvato, tema scuro. *(PR #15.)*
- [ ] **`receipt-items`** — il pezzo difficile: righe prodotto ricostruite dai **bounding box** dell'OCR (banda `y` per la riga, colonne `x` per separare descrizione e prezzo), casi sporchi (quantità `2 X 1,50`, prodotti a peso, sconti, righe di continuazione), validazione somma righe ≈ totale, correzione manuale delle righe, dizionario locale parola-chiave → categoria come seed statico bundle (modello di `issuer-seed`) con fuzzy match, e **apprendimento locale**: la correzione dell'utente diventa una mappatura persistita che vale per gli scontrini successivi.
- [ ] **`receipt-ai-normalize`** — normalizzazione dei nomi prodotto (`PAST BARILLA 500` → `pasta`, categoria) via Claude, **opt-in e degradabile**: chiave dell'utente in `SecureStorage`, si inviano **solo le descrizioni** (mai immagine, totale, esercente o data), output strutturato, una chiamata in batch per scontrino, **cache-first** sulla tabella delle mappature (dopo qualche settimana la spesa abituale è mappata e le chiamate crollano a zero). Senza chiave o senza rete lo scontrino si salva comunque coi nomi grezzi. Categorie d'errore comprensibili sul modello di `maui-backup-error-state`.
- [ ] **`receipt-insights`** — quattro viste in pura SQL: **top alimenti** (frequenza e spesa), **top categorie**, **spesa per negozio/mese**, **andamento prezzo** di un prodotto (sparkline SkiaSharp, già in casa). Le prime tre reggono anche con righe imperfette; la quarta è la più esigente e misura la qualità della change 2.

---

## v2 — Backend e sincronizzazione (rimandato)

> Da affrontare dopo il rilascio della v1 offline. Elencato qui solo come roadmap; non iniziare finché la v1 non è stabile.

**Backend**
- [ ] **`bootstrap-backend`** — soluzione .NET 10, vertical slice, Dockerfile multi-stage, docker-compose con volume, endpoint `/health`
- [ ] **`data-model-core`** — entità Users, Cards, Issuers, Devices, ChangeLog; migration EF Core; WAL + `busy_timeout`; coda di serializzazione delle scritture
- [ ] **`auth-credentials`** — registrazione e login, hashing password, JWT, refresh token con rotazione, registro device e revoca per device
- [ ] **`auth-google`** — validazione `id_token` via JWKS, controllo `aud`/`iss`/`exp`/`nonce`, account linking
- [ ] **`sync-api`** — query `GetChangesSince`, comando composito `PushChanges` con esito per operazione, idempotenza via `OperationId`, last-write-wins *(partire da `/opsx:explore`)*
- [ ] **`issuer-catalog`** — catalogo emittenti versionato lato server, pull incrementale (sostituisce/estende il seed statico)
- [ ] **`share-links`** *(opzionale)* — link con snapshot della carta, redemption, `assetlinks.json`, landing page, rate limit

**Client v2**
- [ ] **`maui-auth`** — login online (credenziali + Google PKCE), gestione token in `SecureStorage`, sblocco biometrico che decifra i token
- [ ] **`maui-sync-client`** — outbox locale, cursore `Seq`, retry con backoff, gestione refresh token scaduto (sola lettura)

**Chiusura**
- [ ] **`deploy-hardening`** — reverse proxy con TLS automatico, healthcheck, backup schedulato con `VACUUM INTO`, restart policy

---

## Note tecniche di riferimento

- **Scanner vs rendering**: due funzioni distinte. Lettura con ML Kit (più affidabile di ZXing su Android per codici stampati/plastificati); riproduzione con ZXing.Net + luminosità max e blocco autorotazione/timeout. Se il lettore cassa è laser alcuni schermi non vengono letti → prevedere sempre il codice in chiaro come fallback.
- **Condivisione via QR (v1)**: il QR incapsula uno *snapshot* della carta, non un riferimento — quindi funziona anche se il mittente poi la cancella, ed è per costruzione offline. Attenzione alla dimensione del payload (tenerlo compatto: campi corti, eventualmente Base64/GZip) per non generare un QR troppo denso da leggere.
- **Perché tombstone e Id client-generati già in v1**: costano poco ora e rendono indolore l'aggancio della sync in v2; introdurli dopo richiederebbe una migrazione dei dati locali degli utenti.
- **Build & auto-update (v1)**: la distribuzione è **fuori dal Play Store**, quindi serve una firma stabile (keystore custodito come secret CI, mai nel repo) e l'aggiornamento self-hosted. Il "server" per l'update è solo **hosting statico** di un file `latest.json` + APK (può bastare GitHub Releases o il reverse proxy che arriverà in v2) — non è il backend applicativo. L'installazione richiede il permesso *"Installa app sconosciute"* concesso dall'utente. **Sempre** verificare `sha256` (o la firma) dell'APK scaricato prima di installarlo, per evitare update manomessi.
- **SQLite backend (v2)**: un solo writer (WAL + busy_timeout + coda). Niente scalabilità orizzontale senza passare a Postgres. File `.db`+`.db-wal`+`.db-shm` su volume Docker dedicato. Backup a caldo con `VACUUM INTO`.
- **CORS**: non pertinente per il client MAUI nativo. Rilevante solo con un eventuale frontend web futuro.

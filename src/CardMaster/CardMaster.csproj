<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<!-- CardMaster v1: solo Android (vedi PLAN.md / openspec config) -->
		<TargetFrameworks>net10.0-android</TargetFrameworks>

		<!-- Note for MacCatalyst:
		The default runtime is maccatalyst-x64, except in Release config, in which case the default is maccatalyst-x64;maccatalyst-arm64.
		When specifying both architectures, use the plural <RuntimeIdentifiers> instead of the singular <RuntimeIdentifier>.
		The Mac App Store will NOT accept apps with ONLY maccatalyst-arm64 indicated;
		either BOTH runtimes must be indicated or ONLY macatalyst-x64. -->
		<!-- For example: <RuntimeIdentifiers>maccatalyst-x64;maccatalyst-arm64</RuntimeIdentifiers> -->

		<OutputType>Exe</OutputType>
		<RootNamespace>CardMaster</RootNamespace>
		<UseMaui>true</UseMaui>
		<SingleProject>true</SingleProject>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>

		<!-- Enable XAML source generation for faster build times and improved performance.
		     This generates C# code from XAML at compile time instead of runtime inflation.
		     To disable, remove this line.
		     For individual files, you can override by setting Inflator metadata:
		       <MauiXaml Update="MyPage.xaml" Inflator="Default" /> (reverts to defaults: Runtime for Debug, XamlC for Release)
		       <MauiXaml Update="MyPage.xaml" Inflator="Runtime" /> (force runtime inflation) -->
		<MauiXamlInflator>SourceGen</MauiXamlInflator>

		<!-- Display name -->
		<ApplicationTitle>CardMaster</ApplicationTitle>

		<!-- App Identifier -->
		<ApplicationId>com.cardmaster.app</ApplicationId>

		<!-- Versions -->
		<ApplicationDisplayVersion>1.0</ApplicationDisplayVersion>
		<ApplicationVersion>1</ApplicationVersion>

		<!-- To develop, package, and publish an app to the Microsoft Store, see: https://aka.ms/MauiTemplateUnpackaged -->
		<WindowsPackageType>None</WindowsPackageType>

		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'ios'">15.0</SupportedOSPlatformVersion>
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'maccatalyst'">15.0</SupportedOSPlatformVersion>
		<!-- API 23 (Android 6.0): richiesto da Keystore AES-GCM per la chiave del DB -->
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">23.0</SupportedOSPlatformVersion>
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows'">10.0.17763.0</SupportedOSPlatformVersion>
		<TargetPlatformMinVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows'">10.0.17763.0</TargetPlatformMinVersion>
	</PropertyGroup>

	<!-- Android Release: produce un APK e firma SOLO se un keystore è fornito (es. dalla CI via -p:AndroidSigningKeyStore=...).
	     Le build Debug/locali senza keystore restano invariate. -->
	<PropertyGroup Condition="'$(Configuration)' == 'Release' and $([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">
		<AndroidPackageFormat>apk</AndroidPackageFormat>
		<AndroidKeyStore Condition="'$(AndroidSigningKeyStore)' != ''">true</AndroidKeyStore>
	</PropertyGroup>

	<ItemGroup>
		<!-- App Icon -->
		<MauiIcon Include="Resources\AppIcon\appicon.svg" ForegroundFile="Resources\AppIcon\appiconfg.svg" Color="#E07B1A" />

		<!-- Splash Screen -->
		<MauiSplashScreen Include="Resources\Splash\splash.svg" Color="#E07B1A" BaseSize="128,128" />

		<!-- Images -->
		<MauiImage Include="Resources\Images\*" />

		<!-- Custom Fonts -->
		<MauiFont Include="Resources\Fonts\*" />

		<!-- Raw Assets (also remove the "Resources\Raw" prefix) -->
		<MauiAsset Include="Resources\Raw\**" LogicalName="%(RecursiveDir)%(Filename)%(Extension)" />
	</ItemGroup>

	<ItemGroup>
		<!-- Lettura assistita degli scontrini: SDK ufficiale Anthropic.
		     La chiave API è dell'utente e vive in SecureStorage: nessuna credenziale
		     nel pacchetto o nel repository (vedi openspec/specs/ai-credentials). -->
		<PackageReference Include="Anthropic" Version="12.45.0" />
		<PackageReference Include="BarcodeScanning.Native.Maui" Version="3.1.0" />
		<!-- OCR scontrini: ML Kit Text Recognition con modello INCLUSO nell'APK (com.google.mlkit),
		     non la variante play-services che lo scarica al primo uso: serve il riconoscimento
		     funzionante al primo avvio senza rete e senza dipendere dai Play Services del device
		     (distribuzione fuori dal Play Store). Prezzo: qualche MB di APK. -->
		<PackageReference Include="Xamarin.Google.MLKit.TextRecognition" Version="116.0.1.8" Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'" />
		<!-- 10.0.100 porta AndroidX Fragment 1.9.0, incompatibile con
		     Fragment.Ktx 1.8.9.3 usato da BarcodeScanning.Native.Maui 3.1.0:
		     R8 genera una classe FragmentKt duplicata. Manteniamo la linea
		     MAUI precedente finché BarcodeScanning non allinea AndroidX. -->
		<PackageReference Include="Microsoft.Maui.Controls" Version="10.0.90" />
		<!-- Backup su Google Drive: schedulazione periodica in background -->
		<PackageReference Include="Xamarin.AndroidX.Work.Runtime" Version="2.11.2.1" Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'" />
		<PackageReference Include="Microsoft.Extensions.Logging.Debug" Version="10.0.11" />
		<PackageReference Include="sqlite-net-base" Version="1.9.172" />
		<PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" Version="3.0.5" />
		<PackageReference Include="ZXing.Net" Version="0.16.11" />
		<PackageReference Include="ZXing.Net.Bindings.SkiaSharp" Version="0.16.24" />
	</ItemGroup>

</Project>
